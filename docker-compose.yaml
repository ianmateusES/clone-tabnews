services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    # volumes:
    #   - .:/app
    networks:
      - tabnews

  database:
    image: postgres:16.0-alpine3.18
    env_file: .env.example
    ports:
      - 5432:5432
    networks:
      - tabnews

networks:
  tabnews:
    driver: bridge
